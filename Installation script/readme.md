***Installation of Kubernetes for CKA***
- Install the Kubelet, Kubectl and Kubeadm processes with install_kube.sh script if the host os is debian in Master and Worker Nodes
- Install containerd in Master and Worker Nodes
- Install the Kubernetes cluster with 'kubeadm init' command